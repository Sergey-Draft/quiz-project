/* таймер для хардкор мода */



/* const hardCore = () => {
  if (hard) {
     
    let x = 10; 
    countdown();
    function countdown() {  
      document.querySelector('.rocket').innerHTML = x;
      x--; 
      if (x < 0) {
        clearTimeout(timer); 
        document.querySelector('.next').click()
      }
      else {
        timer = setTimeout(countdown, 1000);
      }
    }
  }
}

export {hardCore, hard}; */